[project]
name = "ui-tester"
version = "0.1.0"
description = "AI-powered UI testing swarm using Claude Agent SDK and Playwright"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
authors = [
    {name = "hipjim"},
]
keywords = ["testing", "ai", "playwright", "claude", "ui-testing", "qa"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Testing",
]
dependencies = [
    "claude-agent-sdk",
    "httpx>=0.27",
]

[project.urls]
Homepage = "https://github.com/hipjim/ui-tester"
Repository = "https://github.com/hipjim/ui-tester"
Issues = "https://github.com/hipjim/ui-tester/issues"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["ui_tester*"]

[project.scripts]
ui-tester = "ui_tester.cli:main"
